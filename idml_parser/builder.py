import os
import pathlib as path
import sys
import shutil as shut
import logging as log
import datetime as dt

def build_html(spreads,stories,styles,output_folder):
    """
    Transform the XML assets into an HTML/CSS file
    INPUT : spreads(obj), stories(obj), styles(obj), output_folder(str)
    :rtype: HTML file
    """
    pass
